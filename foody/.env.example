# Foody 部署环境变量示例
# 本地开发不用管这个文件；部署到云端时，把这些填进平台的「Environment」设置里。

# 生产模式：开启 HTTPS 安全 Cookie + 信任反向代理（取真实访客 IP 做限流）
NODE_ENV=production

# 监听端口：大多数平台会自动注入 PORT，通常无需手动填
# PORT=3000

# 持久化目录：部署在云端时指向「挂载的持久磁盘」，否则重启 / 重新部署会丢数据！
# 例：Render 把磁盘挂在 /var/data 时——
# FOODY_DATA_DIR=/var/data/db
# FOODY_UPLOAD_DIR=/var/data/uploads

# ---------------- AI 一键生成网页（选填、免费）----------------
# 「我的网页」里的「✨ AI 一键生成」需要一个 AI key 才能用；不配也完全不影响其它功能。
# 推荐用 Google Gemini 免费档（额度够日常用、不花钱）：
#   1. 打开 https://aistudio.google.com/apikey （用 Google 账号登入）
#   2. 点「Create API key」，复制那串 key
#   3. 在 foody 文件夹建一个 .env 文件，写上下面这行（去掉开头的 #、换成你的 key）
#   4. 重启 start-foody.bat 即可
# FOODY_AI_KEY=把你的key贴这里
# 想换模型可改这行（默认 gemini-2.0-flash；也可填 gemini-2.5-flash 等）：
# FOODY_AI_MODEL=gemini-2.0-flash
